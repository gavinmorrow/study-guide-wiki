const logger = require("../../../logger");
const updateGuideTitle = (ws, data, session) => {
	logger.trace("Updating guide title", data);

	// TODO: update guide title
};
module.exports = updateGuideTitle;
